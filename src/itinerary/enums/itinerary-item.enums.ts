export enum ItineraryItemType {
  Sightseeing = 'sightseeing',
  Meal = 'meal',
  Transfer = 'transfer',
  Other = 'other',
}

export enum ItineraryItemStatus {
  Planned = 'planned',
  Confirmed = 'confirmed',
  Cancelled = 'cancelled',
  Completed = 'completed',
}
