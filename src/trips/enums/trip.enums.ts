export enum TripStatus {
  Planning = 'planning',
  InProgress = 'in-progress',
  Completed = 'completed',
}

export enum TripType {
  Leisure = 'leisure',
  Business = 'business',
  Family = 'family',
  RoadTrip = 'road-trip',
  Adventure = 'adventure',
  Other = 'other',
}

export enum TripClimate {
  Warm = 'warm',
  Cold = 'cold',
  Temperate = 'temperate',
  Tropical = 'tropical',
}
